import { EnhancerOptions } from "@redux-devtools/extension"

export const reduxDevToolsOptions: EnhancerOptions = {
    // actionsDenylist: blacklist,
    // actionsAllowlist: whitelist,
    name: "Cloudflare Images App",
}
